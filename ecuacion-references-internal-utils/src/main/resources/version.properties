# The ecuacion-references reactor's own version, read via VersionUtil.getVersion("").
# Placed here (rather than duplicated per reference-xxx site) because every
# ecuacion-references-* site module shares this same parent-inherited version — there is no
# per-site version distinct from it.
version=@project.version@
